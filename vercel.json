{
  "version": 2,
  "installCommand": "npm install && cd admin-dashboard && npm install",
  "buildCommand": "cd admin-dashboard && npm run build",
  "functions": {
    "server.js": {
      "includeFiles": "admin-dashboard/dist/**"
    }
  },
  "builds": [
    {
      "src": "server.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "server.js" },
    { "src": "/admin(.*)", "dest": "server.js" }
  ]
}
